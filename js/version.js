// The game's version, shown in the header. When releasing: bump this and
// package.json, add a CHANGELOG.md entry, then tag the commit (git tag vX.Y.Z).
export const VERSION = '0.6.0';
export const RELEASE_DATE = '2026-09-25';
